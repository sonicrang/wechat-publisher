# 微信公众号发布配置

# 微信公众平台 AppID（必填）
# 从微信公众平台 → 设置与开发 → 基本配置 获取
wechat:
  appId: "your-app-id"
  
  # 微信公众平台 AppSecret（必填）
  # 从微信公众平台 → 设置与开发 → 基本配置 → 开发者密码 获取
  appSecret: "your-app-secret"

# 默认作者（可选）
# 发布文章时如果未指定 -a 参数，将使用此作者
author: "Your Name"